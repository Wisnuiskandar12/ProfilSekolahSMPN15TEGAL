<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.profilsekolahsmpn15tegal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfilSekolahSMPN15TEGAL">
        <activity android:name=".GalleryActivity"></activity>
        <activity android:name=".FasilitasActivity" />
        <activity android:name=".GuruActivity" />
        <activity android:name=".PrestasiActivity" />
        <activity android:name=".EkstraActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>